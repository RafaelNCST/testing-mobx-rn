import styled from 'styled-components/native';
import { SafeAreaView } from "react-native"

export const BackGroundScreen: typeof SafeAreaView = styled.SafeAreaView`
  flex: 1;
  background-color: #447dda;
`;
